# ───────── Hugo (Blog) ─────────
# IMPORTANTE: Como React usa la carpeta "public" como fuente,
# debemos especificar que solo queremos ignorar la "public" DE HUGO.
# Ajusta "blog_hugo/" si le pusiste otro nombre a la carpeta.
blog_hugo/public/
blog_hugo/resources/
hugo_stats.json
.hugo_build.lock

# ───────── React / Vite (Portafolio) ─────────
# Dependencias
node_modules/

# Build de producción (Lo que genera Vite)
dist/
dist-ssr/

# Variables de entorno (Secretos - ¡NUNCA SUBIR!)
.env
.env.local
.env.*.local

# Logs de depuración
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing (si usas Vitest/Jest)
coverage/

# ───────── SO / Sistema ─────────
.DS_Store
Thumbs.db
.os_*

# ───────── IDEs / Editores ─────────
.idea/
.vscode/
*.code-workspace
*.swp
*.swo